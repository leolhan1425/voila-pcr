/**
 * Auth API helpers — stubs for Clerk/Supabase integration.
 *
 * TODO: Replace with real Clerk or Supabase auth calls once configured.
 */

/**
 * Check current authentication status.
 * @returns {Promise<object|null>} User object or null
 */
export async function checkAuth() {
  // TODO: Clerk/Supabase — check session, return user object
  return null
}

/**
 * Get the current session token.
 * @returns {Promise<object|null>} Session object or null
 */
export async function getSession() {
  // TODO: Clerk/Supabase — return current session with JWT
  return null
}
